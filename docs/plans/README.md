# Plans

One short plan per feature batch, written by `/kickoff` and `/new-feature`
before implementation. Name files `NNN-<slug>.md` (e.g. `001-foundation.md`).

A plan is 1–2 pages: what's being built (business terms), the tables/policies
it needs, the screens, the tests that prove it, and what is explicitly NOT in
this batch.
